<%= form_for(group_library) do |f| %>
  <% if group_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_library.errors.count, "error") %> prohibited this group_library from being saved:</h2>

      <ul>
      <% group_library.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
